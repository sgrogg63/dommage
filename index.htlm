<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Accueil Firefox</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: url('fond.jpg') no-repeat center center fixed;
      background-size: cover;
      color: white;
      text-align: center;
    }
    .overlay {
      background-color: rgba(0, 0, 0, 0.6);
      padding: 50px;
      min-height: 100vh;
    }
    h1 {
      font-size: 3em;
      margin-bottom: 0.2em;
    }
    .clock {
      font-size: 2em;
      margin-bottom: 1em;
    }
    .quote {
      font-style: italic;
      margin-bottom: 2em;
    }
    .shortcuts a {
      display: inline-block;
      margin: 10px;
      padding: 10px 20px;
      background-color: #ff4444;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      transition: background 0.3s;
    }
    .shortcuts a:hover {
      background-color: #ff0000;
    }
    .search-box {
      margin-bottom: 30px;
    }
    .search-box input {
      padding: 10px;
      width: 300px;
      border: none;
      border-radius: 5px 0 0 5px;
      font-size: 16px;
    }
    .search-box button {
      padding: 10px 15px;
      border: none;
      background-color: #ff4444;
      color: white;
      font-size: 16px;
      border-radius: 0 5px 5px 0;
      cursor: pointer;
    }
    .search-box button:hover {
      background-color: #ff0000;
    }
  </style>
</head>
<body>
  <div class="overlay">
    <h1>Bienvenue, Pierre</h1>
    <div class="clock" id="clock"></div>
    <div class="quote" id="quote">"Chaque jour est une nouvelle aventure."</div>

    <div class="search-box">
      <form action="https://www.google.com/search" method="get" target="_blank">
        <input type="text" name="q" placeholder="Rechercher sur Google..." />
        <button type="submit">🔍</button>
      </form>
    </div>

    <div class="shortcuts">
      <a href="https://www.google.com">Google</a>
      <a href="https://www.youtube.com">YouTube</a>
      <a href="https://www.wikipedia.org">Wikipedia</a>
      <a href="https://www.lemonde.fr">Le Monde</a>
    </div>
  </div>

  <script>
    function updateClock() {
      const now = new Date();
      const hours = now.getHours().toString().padStart(2, '0');
      const minutes = now.getMinutes().toString().padStart(2, '0');
      document.getElementById('clock').textContent = `${hours}:${minutes}`;
    }
    setInterval(updateClock, 1000);
    updateClock();

    const quotes = [
      "Chaque jour est une nouvelle aventure.",
      "Le futur appartient à ceux qui croient en la beauté de leurs rêves.",
      "Reste curieux, reste inspiré.",
      "La magie commence là où commence l’imagination."
    ];
    document.getElementById('quote').textContent = quotes[Math.floor(Math.random() * quotes.length)];
  </script>
</body>
</html>

